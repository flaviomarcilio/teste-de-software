# Use in-memory H2 for integration tests
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
quarkus.datasource.jdbc.driver=org.h2.Driver
quarkus.datasource.username=sa
quarkus.datasource.password=sa

# Let Hibernate create schema for tests
quarkus.hibernate-orm.database.generation=drop-and-create

# Disable Flyway during tests to avoid requiring migrations
quarkus.flyway.migrate-at-start=false
